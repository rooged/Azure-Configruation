# SampleWebAppBackend
This is meant to run as the backend for the SampleWebApp and SampleConsoleApp. Run this project then the endpoints in SampleWebApp and SampleConsoleApp will be available.

If running locally, replace the user secrets with your own values.